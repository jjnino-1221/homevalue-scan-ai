# Netlify Configuration
# Deployment settings for mobile-valuation app

[build]
  # No build command needed (static HTML)
  command = "echo 'No build required'"
  publish = "."

# Redirects for SPA routing (not needed for Phase 1)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
